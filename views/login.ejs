<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TERMINAL LOGIN // IUT MART</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        bg: '#09090b',
                        surface: '#18181b',
                        border: '#27272a',
                        bull: '#00e396',
                        bear: '#ff4560',
                        text: {
                            primary: '#ffffff',
                            secondary: '#a1a1aa'
                        },
                        action: '#3b82f6'
                    },
                    fontFamily: {
                        mono: ['"JetBrains Mono"', 'monospace'],
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <link
        href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #09090b;
            color: #ffffff;
            font-family: 'JetBrains Mono', monospace;
        }

        /* Subtle grid background */
        .bg-grid {
            background-size: 40px 40px;
            background-image: linear-gradient(to right, #27272a 1px, transparent 1px),
                linear-gradient(to bottom, #27272a 1px, transparent 1px);
            mask-image: radial-gradient(circle at center, black 40%, transparent 100%);
        }
    </style>
</head>

<body class="min-h-screen flex items-center justify-center relative overflow-hidden">
    <!-- Background Grid Effect -->
    <div class="absolute inset-0 bg-grid opacity-20 pointer-events-none"></div>

    <div class="w-full max-w-md p-6 relative z-10">
        <!-- Brand/Header -->
        <div class="text-center mb-8">
            <div
                class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-surface border border-border mb-4 shadow-lg shadow-action/10 group">
                <i class="fas fa-cube text-2xl text-action group-hover:scale-110 transition duration-300"></i>
            </div>
            <h1 class="text-2xl font-bold tracking-tight text-white mb-2">ACCESS TERMINAL</h1>
            <p class="text-text-secondary text-xs tracking-wider">SECURE CONNECTION REQUIRED</p>
        </div>

        <div class="bg-surface border border-border rounded-lg shadow-2xl overflow-hidden backdrop-blur-sm">
            <!-- Terminal Header Bar -->
            <div class="bg-bg/50 border-b border-border px-4 py-2 flex items-center space-x-2">
                <div class="w-3 h-3 rounded-full bg-bear/50"></div>
                <div class="w-3 h-3 rounded-full bg-yellow-500/50"></div>
                <div class="w-3 h-3 rounded-full bg-bull/50"></div>
                <span class="ml-auto text-[10px] text-text-secondary">auth.exe</span>
            </div>

            <div class="p-8">
                <% if (locals.error && locals.error.length> 0) { %>
                    <div
                        class="mb-6 bg-bear/10 border border-bear/20 text-bear px-4 py-3 rounded text-xs flex items-center">
                        <i class="fas fa-exclamation-triangle mr-2"></i>
                        <span>
                            <%= error %>
                        </span>
                    </div>
                    <% } %>

                        <form action="/auth/login" method="POST" class="space-y-5">
                            <div>
                                <label class="block text-xs font-bold text-text-secondary mb-1.5 uppercase">Student ID /
                                    Email</label>
                                <div class="relative group">
                                    <span
                                        class="absolute inset-y-0 left-0 pl-3 flex items-center text-text-secondary group-focus-within:text-action transition">
                                        <i class="fas fa-user-shield text-sm"></i>
                                    </span>
                                    <input type="email" name="email" required placeholder="student@iut-dhaka.edu"
                                        class="w-full bg-bg border border-border text-white text-sm rounded pl-10 pr-4 py-2.5 focus:border-action focus:ring-1 focus:ring-action outline-none transition placeholder-text-secondary/30 font-mono">
                                </div>
                            </div>

                            <div>
                                <div class="flex justify-between items-center mb-1.5">
                                    <label
                                        class="block text-xs font-bold text-text-secondary uppercase">Passcode</label>
                                </div>
                                <div class="relative group">
                                    <span
                                        class="absolute inset-y-0 left-0 pl-3 flex items-center text-text-secondary group-focus-within:text-action transition">
                                        <i class="fas fa-key text-sm"></i>
                                    </span>
                                    <input type="password" name="password" required placeholder="••••••••"
                                        class="w-full bg-bg border border-border text-white text-sm rounded pl-10 pr-4 py-2.5 focus:border-action focus:ring-1 focus:ring-action outline-none transition placeholder-text-secondary/30 font-mono">
                                </div>
                            </div>

                            <button type="submit"
                                class="w-full bg-action hover:bg-blue-600 text-white font-bold py-2.5 rounded transition duration-200 shadow-lg shadow-action/20 flex items-center justify-center space-x-2 text-sm">
                                <span>ESTABLISH UPLINK</span>
                                <i class="fas fa-arrow-right text-xs"></i>
                            </button>
                        </form>

                        <div class="relative my-8">
                            <div class="absolute inset-0 flex items-center">
                                <div class="w-full border-t border-border"></div>
                            </div>
                            <div class="relative flex justify-center text-xs">
                                <span class="px-2 bg-surface text-text-secondary">OR CONNECT VIA</span>
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-4">
                            <a href="/auth/google"
                                class="flex items-center justify-center px-4 py-2.5 border border-border rounded bg-white hover:bg-gray-100 transition duration-200 group">
                                <img src="https://www.svgrepo.com/show/475656/google-color.svg" class="h-4 w-4 mr-2"
                                    alt="Google">
                                <span class="text-xs font-bold text-gray-900 font-sans">Google</span>
                            </a>
                            <a href="/auth/github"
                                class="flex items-center justify-center px-4 py-2.5 border border-border rounded bg-[#24292e] hover:bg-[#2f363d] transition duration-200">
                                <i class="fab fa-github text-white text-lg mr-2"></i>
                                <span class="text-xs font-bold text-white font-sans">GitHub</span>
                            </a>
                        </div>
            </div>

            <div class="bg-bg/50 px-8 py-4 border-t border-border text-center">
                <p class="text-xs text-text-secondary">
                    New User? <a href="/auth/register"
                        class="text-action hover:text-white font-bold transition">Initialise Protocol</a>
                </p>
            </div>
        </div>

        <div class="mt-8 text-center">
            <p class="text-[10px] text-text-secondary/50 font-mono">
                <i class="fas fa-shield-alt mr-1"></i> SECURE SERVER v2.4.0 // ENCRYPTED
            </p>
        </div>
    </div>
</body>

</html>